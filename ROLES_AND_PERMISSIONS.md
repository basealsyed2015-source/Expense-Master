# 🔐 الأدوار والصلاحيات - نظام إدارة التمويل

**تاريخ التحديث:** 22 ديسمبر 2025  
**الحالة:** ✅ تم تصحيح جميع الأدوار والصلاحيات

---

## 📋 جدول المستخدمين وبيانات الدخول

| # | الدور | اسم المستخدم | كلمة المرور | الاسم الكامل | البريد الإلكتروني | عدد الصلاحيات |
|---|-------|-------------|-------------|--------------|------------------|--------------|
| 1 | 👔 **مدير عام** | `superadmin` | `Super@2025` | المدير العام | superadmin@system.sa | **39 صلاحية** |
| 2 | 🏢 **مدير شركة** | `companyadmin` | `Company@2025` | مدير الشركة | company@tamweel.sa | **21 صلاحية** |
| 3 | 👁️ **مشرف** | `supervisor` | `Supervisor@2025` | المشرف | supervisor@tamweel.sa | **11 صلاحية** |
| 4 | 👤 **موظف** | `employee` | `Employee@2025` | الموظف | employee@tamweel.sa | **10 صلاحيات** |

---

## 🎯 الأدوار والصلاحيات التفصيلية

### 1️⃣ المدير العام (Super Admin)

```
👤 اسم المستخدم: superadmin
🔑 كلمة المرور: Super@2025
🎯 الدور: superadmin
📊 الصلاحيات: 39 صلاحية (جميع الصلاحيات)
```

**الصلاحيات الكاملة:**
- ✅ **لوحة التحكم:** عرض كامل + إحصائيات
- ✅ **العملاء:** عرض، إضافة، تعديل، حذف
- ✅ **طلبات التمويل:** عرض، إضافة، تعديل، حذف، قبول، رفض
- ✅ **البنوك:** عرض، إضافة، تعديل، حذف
- ✅ **نسب التمويل:** عرض، إضافة، تعديل، حذف
- ✅ **الاشتراكات:** عرض، إضافة، تعديل، حذف، قبول، رفض
- ✅ **الباقات:** عرض، إضافة، تعديل، حذف
- ✅ **المستخدمين:** عرض، إضافة، تعديل، حذف، إدارة الصلاحيات
- ✅ **التقارير:** عرض، تصدير
- ✅ **الحاسبة:** استخدام، تصدير

---

### 2️⃣ مدير الشركة (Company Admin)

```
👤 اسم المستخدم: companyadmin
🔑 كلمة المرور: Company@2025
🎯 الدور: companyadmin
📊 الصلاحيات: 21 صلاحية (إدارة الشركة)
```

**الصلاحيات:**
- ✅ **لوحة التحكم:** عرض + إحصائيات
- ✅ **العملاء:** عرض، إضافة، تعديل، حذف
- ✅ **طلبات التمويل:** عرض، إضافة، تعديل، حذف
- ✅ **البنوك:** عرض فقط
- ✅ **نسب التمويل:** عرض فقط
- ✅ **التقارير:** عرض، تصدير
- ✅ **الحاسبة:** استخدام، تصدير
- ✅ **المستخدمين:** عرض، إضافة، تعديل
- ❌ **لا يمكنه:** حذف المستخدمين، إدارة صلاحيات المستخدمين، تعديل البنوك، تعديل نسب التمويل

---

### 3️⃣ المشرف (Supervisor)

```
👤 اسم المستخدم: supervisor
🔑 كلمة المرور: Supervisor@2025
🎯 الدور: supervisor
📊 الصلاحيات: 11 صلاحية (عرض ومراقبة فقط)
```

**الصلاحيات:**
- ✅ **لوحة التحكم:** عرض + إحصائيات
- ✅ **العملاء:** عرض فقط
- ✅ **طلبات التمويل:** عرض فقط
- ✅ **البنوك:** عرض فقط
- ✅ **نسب التمويل:** عرض فقط
- ✅ **الاشتراكات:** عرض فقط
- ✅ **الباقات:** عرض فقط
- ✅ **المستخدمين:** عرض فقط
- ✅ **التقارير:** عرض فقط
- ✅ **الحاسبة:** استخدام فقط
- ❌ **لا يمكنه:** إضافة، تعديل، حذف أي بيانات

---

### 4️⃣ الموظف (Employee)

```
👤 اسم المستخدم: employee
🔑 كلمة المرور: Employee@2025
🎯 الدور: employee
📊 الصلاحيات: 10 صلاحيات (صلاحيات أساسية)
```

**الصلاحيات:**
- ✅ **لوحة التحكم:** عرض فقط
- ✅ **العملاء:** عرض، إضافة، تعديل
- ✅ **طلبات التمويل:** عرض، إضافة، تعديل
- ✅ **البنوك:** عرض فقط
- ✅ **نسب التمويل:** عرض فقط
- ✅ **الحاسبة:** استخدام فقط
- ❌ **لا يمكنه:** حذف العملاء، حذف الطلبات، الموافقة/الرفض، إدارة المستخدمين

---

## 🔗 رابط تسجيل الدخول

```
https://3000-ijnjky15yjf24gcevexiq-c81df28e.sandbox.novita.ai/login
```

---

## 📊 مقارنة الأدوار

| الميزة / الصفحة | مدير عام | مدير شركة | مشرف | موظف |
|----------------|----------|-----------|------|------|
| **لوحة التحكم** | ✅ كامل | ✅ كامل | ✅ عرض فقط | ✅ عرض فقط |
| **العملاء - عرض** | ✅ | ✅ | ✅ | ✅ |
| **العملاء - إضافة** | ✅ | ✅ | ❌ | ✅ |
| **العملاء - تعديل** | ✅ | ✅ | ❌ | ✅ |
| **العملاء - حذف** | ✅ | ✅ | ❌ | ❌ |
| **طلبات التمويل - عرض** | ✅ | ✅ | ✅ | ✅ |
| **طلبات التمويل - إضافة** | ✅ | ✅ | ❌ | ✅ |
| **طلبات التمويل - تعديل** | ✅ | ✅ | ❌ | ✅ |
| **طلبات التمويل - حذف** | ✅ | ✅ | ❌ | ❌ |
| **طلبات التمويل - موافقة/رفض** | ✅ | ❌ | ❌ | ❌ |
| **البنوك - عرض** | ✅ | ✅ | ✅ | ✅ |
| **البنوك - تعديل** | ✅ | ❌ | ❌ | ❌ |
| **نسب التمويل - عرض** | ✅ | ✅ | ✅ | ✅ |
| **نسب التمويل - تعديل** | ✅ | ❌ | ❌ | ❌ |
| **المستخدمين - عرض** | ✅ | ✅ | ✅ | ❌ |
| **المستخدمين - إدارة** | ✅ | ⚠️ جزئي | ❌ | ❌ |
| **التقارير** | ✅ | ✅ | ✅ | ❌ |
| **الحاسبة** | ✅ | ✅ | ✅ | ✅ |

**الرموز:**
- ✅ متاح بالكامل
- ⚠️ متاح جزئياً
- ❌ غير متاح

---

## 🧪 اختبار تسجيل الدخول

### من المتصفح:
1. افتح: https://3000-ijnjky15yjf24gcevexiq-c81df28e.sandbox.novita.ai/login
2. اختر أحد الحسابات من الجدول أعلاه
3. أدخل اسم المستخدم وكلمة المرور
4. اضغط "تسجيل الدخول"

### من curl:
```bash
# اختبار المدير العام
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"superadmin","password":"Super@2025"}'

# اختبار مدير الشركة
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"companyadmin","password":"Company@2025"}'

# اختبار المشرف
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"supervisor","password":"Supervisor@2025"}'

# اختبار الموظف
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"employee","password":"Employee@2025"}'
```

---

## 📝 ملاحظات مهمة

### ⚠️ للتطوير فقط (Development Only)
- كلمات المرور غير مشفرة في قاعدة البيانات
- التوكن بسيط (`btoa` encoding)
- **لا يجب** استخدامه في الإنتاج بدون تحسين

### ✅ للإنتاج (Production Ready)
يجب تطبيق:
1. تشفير كلمات المرور باستخدام `bcrypt`
2. JWT حقيقي مع `hono/jwt`
3. Secret Key في `.env`
4. وقت انتهاء للتوكن (Expiration)
5. Refresh Token
6. التحقق من التوكن في كل API request

---

## 📂 هيكل قاعدة البيانات

### جدول الأدوار (roles)
```sql
CREATE TABLE roles (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  role_name TEXT UNIQUE NOT NULL,
  description TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### جدول الصلاحيات (permissions)
```sql
CREATE TABLE permissions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  permission_key TEXT UNIQUE NOT NULL,
  permission_name TEXT NOT NULL,
  description TEXT,
  category TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### جدول ربط الصلاحيات بالأدوار (role_permissions)
```sql
CREATE TABLE role_permissions (
  role_id INTEGER,
  permission_id INTEGER,
  PRIMARY KEY (role_id, permission_id),
  FOREIGN KEY (role_id) REFERENCES roles(id),
  FOREIGN KEY (permission_id) REFERENCES permissions(id)
);
```

### جدول المستخدمين (users)
```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  full_name TEXT,
  email TEXT UNIQUE NOT NULL,
  phone TEXT,
  role_id INTEGER,
  user_type TEXT,
  tenant_id INTEGER,
  is_active INTEGER DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (role_id) REFERENCES roles(id)
);
```

---

## 🎉 النتيجة

**نظام الأدوار والصلاحيات الآن يعمل بشكل صحيح! ✅**

- ✅ 4 أدوار محددة بوضوح
- ✅ صلاحيات مفصلة لكل دور
- ✅ جميع المستخدمين يعملون
- ✅ تسجيل الدخول يعمل لجميع الأدوار
- ✅ الصلاحيات مربوطة بشكل صحيح

---

**آخر تحديث:** 22 ديسمبر 2025  
**Git Commit:** 0fd6701  
**الحالة:** ✅ نظام الأدوار والصلاحيات يعمل بشكل كامل
