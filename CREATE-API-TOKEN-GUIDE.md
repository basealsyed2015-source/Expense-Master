# 🎯 دليل مصور: إنشاء Cloudflare API Token

## 📋 الخطوات التفصيلية

### **الخطوة 1: افتح صفحة API Tokens**
```
https://dash.cloudflare.com/profile/api-tokens
```

---

### **الخطوة 2: Create Token**

في صفحة "API Tokens"، ستجد:
- قائمة بالـ Tokens الموجودة (إن وجدت)
- زر أزرق كبير: **"Create Token"**

**اضغط عليه!**

---

### **الخطوة 3: اختر "Edit Cloudflare Workers"**

ستظهر صفحة بها قوالب (Templates) جاهزة:

```
API token templates
├── Edit zone DNS                    [Use template]
├── Edit Cloudflare Workers          [Use template] ← اختر هذا!
├── Read all resources               [Use template]
├── WordPress                        [Use template]
└── Create Additional Tokens         [Use template]
```

**اضغط على "Use template" بجانب "Edit Cloudflare Workers"**

---

### **الخطوة 4: مراجعة الصلاحيات**

بعد اختيار القالب، ستظهر صفحة مراجعة تحتوي على:

#### **Token name:**
```
(اختياري) مثال: "GenSpark Deployment Token"
```

#### **Permissions:**
```
Account | Cloudflare Pages | Edit
Account | Account Settings | Read
```

هذه الصلاحيات **صحيحة ومناسبة** ✅

#### **Account Resources:**
```
Include | All accounts
```
أو يمكنك اختيار حساب معين إذا كان لديك أكثر من حساب

#### **Client IP Address Filtering:** (اختياري)
```
Is in | [يمكنك تركه فارغاً]
```

#### **TTL (Time to Live):**
```
Start Date: [التاريخ الحالي]
End Date: [يمكنك اختيار تاريخ انتهاء أو تركه مفتوحاً]
```

**نصيحة:** اترك "End Date" فارغاً أو اختر تاريخ بعيد (سنة مثلاً)

---

### **الخطوة 5: Continue to summary**

في أسفل الصفحة:
- اضغط **"Continue to summary"**

---

### **الخطوة 6: مراجعة نهائية**

ستظهر صفحة ملخص بجميع التفاصيل.

تأكد من:
- ✅ الصلاحيات صحيحة (Cloudflare Pages: Edit)
- ✅ الحساب صحيح

ثم اضغط: **"Create Token"**

---

### **الخطوة 7: انسخ الـ Token (مهم جداً!)**

⚠️ **هذه أهم خطوة!**

بعد إنشاء الـ Token، ستظهر صفحة بها:

```
┌─────────────────────────────────────────────────────────┐
│  ✅ API Token created successfully                      │
│                                                         │
│  Token:                                                 │
│  ┌───────────────────────────────────────────────────┐ │
│  │ xxxxxxxxxxxxx_yyyyyyyyyyyyy_zzzzzzzzzzzzzzzzzzzzz │ │ ← انسخ هذا!
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  [Copy]  ← أو اضغط هنا                                 │
└─────────────────────────────────────────────────────────┘

⚠️  Warning: This token will only be shown once.
   Make sure you copy it now!
```

**اضغط على زر "Copy"** أو انسخ النص يدوياً

⚠️ **مهم جداً:**
- هذا الـ Token سيظهر **مرة واحدة فقط**!
- إذا لم تنسخه، ستحتاج لإنشاء token جديد
- احفظه في مكان آمن

---

### **الخطوة 8: تأكيد النسخ**

بعد النسخ، اضغط: **"Done"** أو **"Back to tokens"**

---

## ✅ الآن لديك الـ Token!

النص المنسوخ سيكون بهذا الشكل:
```
xxxxxxxxxxxxx_yyyyyyyyyyyyy_zzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzz
```

---

## 🎯 الخطوة التالية: أضف الـ Token في GenSpark

### **الطريقة 1: عبر GenSpark UI (موصى بها)**

1. في واجهة GenSpark، ابحث عن تبويب **"Deploy"** في الشريط الجانبي
2. ستجد حقل اسمه **"Cloudflare API Key"** أو **"Cloudflare API Token"**
3. **الصق** الـ Token المنسوخ
4. اضغط **"Save"** / **"حفظ"**

---

### **الطريقة 2: عبر Terminal**

إذا لم تجد تبويب Deploy، يمكنك استخدام Terminal مباشرة:

```bash
# ضع الـ Token في متغير بيئة
export CLOUDFLARE_API_TOKEN="الصق-التوكن-هنا"

# تحقق من التوكن
cd /home/user/webapp
npx wrangler whoami
```

**النتيجة المتوقعة:**
```
👋 You are logged in with an API Token
```

---

## 🚀 بعد إضافة الـ Token: انشر!

```bash
cd /home/user/webapp

# الطريقة 1: سكريبت تلقائي
./deploy-prod.sh

# أو الطريقة 2: npm
npm run deploy:prod

# أو الطريقة 3: يدوي
npm run build
npx wrangler pages deploy dist --project-name tamweel-calc-prod --branch main
```

---

## 🐛 حل المشاكل الشائعة

### **مشكلة 1: لم أنسخ الـ Token!**
```
الحل: أنشئ token جديد
1. اذهب إلى: https://dash.cloudflare.com/profile/api-tokens
2. كرر الخطوات
3. انسخ الـ Token هذه المرة!
```

### **مشكلة 2: "Invalid API Token"**
```
الأسباب المحتملة:
- نسخت Token قديم
- الصلاحيات غير صحيحة
- Token منتهي الصلاحية

الحل: أنشئ token جديد
```

### **مشكلة 3: "You are not authenticated"**
```
الحل:
1. تأكد من نسخ Token كاملاً (بدون مسافات زائدة)
2. جرب: export CLOUDFLARE_API_TOKEN="xxx"
3. ثم: npx wrangler whoami
```

---

## 📊 ملخص الخطوات (Quick Reference)

```
1. https://dash.cloudflare.com/profile/api-tokens
2. Create Token
3. Use template: "Edit Cloudflare Workers"
4. Continue to summary
5. Create Token
6. ⚠️ انسخ الـ Token (مرة واحدة فقط!)
7. GenSpark → Deploy → Cloudflare API Key → الصق → حفظ
8. Terminal: ./deploy-prod.sh
```

---

## ✅ Checklist

- [ ] فتحت https://dash.cloudflare.com/profile/api-tokens
- [ ] ضغطت "Create Token"
- [ ] اخترت "Edit Cloudflare Workers"
- [ ] راجعت الصلاحيات
- [ ] ضغطت "Continue to summary"
- [ ] ضغطت "Create Token"
- [ ] **نسخت الـ Token** ⚠️
- [ ] حفظت Token في مكان آمن
- [ ] أضفت Token في GenSpark Deploy
- [ ] جاهز للنشر!

---

## 🎉 جاهز!

بعد إكمال الخطوات:
```bash
cd /home/user/webapp
./deploy-prod.sh
```

**🚀 انطلق!**
